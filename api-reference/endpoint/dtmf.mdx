---
title: Create DTMF Action
api: 'POST https://prod-contactswing-fastapi-962560522883.us-central1.run.app/v2/actions'
description: Create DTMF (Dual-Tone Multi-Frequency) actions for IVR keypad input handling.
---

### Authorizations
<ParamField 
  header="Authorization" 
  type="string" 
  required
>
Enter your ContactSwing JWT Bearer token.
</ParamField>

### Request Body

<ParamField path="name" type="string" required>
Action display name. Example: `DTMF Action`
</ParamField>

<ParamField path="description" type="string" required>
Action purpose. Example: `DTMF Action Test`
</ParamField>

<ParamField path="action_timing" type="string" required>
When to trigger: `"DURING"`
</ParamField>

<ParamField path="type" type="string" required>
Action type: `"DTMF_ACTION"`
</ParamField>

<ParamField path="webhook" type="object" required>
DTMF webhook configuration
</ParamField>

### DTMF Configuration

<ParamField path="webhook.mandatory" type="boolean">
Require DTMF input before proceeding
</ParamField>

<ParamField path="webhook.description" type="string">
DTMF prompt/description. Example: `DTMF Action Test`
</ParamField>

### Response

**201 Created** - DTMF action created successfully

<ResponseField name="ctx_id" type="string">Request context ID</ResponseField>
<ResponseField name="data" type="object">
  <ResponseField name="action_id" type="string">New DTMF action ID</ResponseField>
  <ResponseField name="name" type="string">Action name</ResponseField>
  <ResponseField name="type" type="string">"DTMF_ACTION"</ResponseField>
</ResponseField>

**400 Bad Request** - Invalid DTMF webhook configuration

**401 Unauthorized** - Invalid token
